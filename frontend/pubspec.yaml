name: feature_toggle_app
description: Feature Toggle — management dashboard.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  flutter_bloc: ^8.1.6
  go_router: ^14.0.0
  fpdart: ^1.1.0
  get_it: ^7.7.0
  flutter_animate: ^4.5.0
  google_fonts: ^6.1.0
  http: ^1.2.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo_no_text.jpeg
